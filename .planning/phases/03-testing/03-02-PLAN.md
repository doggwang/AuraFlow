---
phase: 03-testing
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/audio-engine.test.js
  - tests/face-analyzer.test.js
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "AudioEngine has unit tests for noise buffer creation"
    - "AudioEngine has unit tests for mood-based gain adjustment"
    - "FaceAnalyzer has unit tests for EAR calculation"
    - "FaceAnalyzer has unit tests for mood classification thresholds"
  artifacts:
    - path: tests/audio-engine.test.js
      provides: AudioEngine unit tests
      min_lines: 50
    - path: tests/face-analyzer.test.js
      provides: FaceAnalyzer unit tests
      min_lines: 50
---

<objective>
Add core unit tests for AudioEngine and FaceAnalyzer classes

Purpose: Ensure the audio synthesis and mood classification logic works correctly by testing key methods and threshold behavior.

Output: tests/audio-engine.test.js, tests/face-analyzer.test.js
</objective>

<context>
@D:/Project/vibe coding/AuraFlow/index.html

**AudioEngine class (lines 1068-1325):**
- `init()` - initializes AudioContext, creates noise sources
- `createWhiteNoiseBuffer(size)` - creates white noise buffer
- `createPinkNoiseBuffer(size)` - creates pink noise buffer using Voss-McCartney algorithm
- `createRainBuffer(size)` - creates rain-like noise
- `createFireBuffer(size)` - creates low-frequency rumble
- `setMood(mood)` - adjusts gains based on mood
- `dispose()` - cleans up audio nodes

**FaceAnalyzer class (lines 1327-1507):**
- `calculateEAR(eye)` - eye aspect ratio calculation
- `calculateMAR(mouth)` - mouth aspect ratio calculation
- `calcBrowEyeRatio(brow, eye)` - brow-to-eye distance ratio
- `dist(p1, p2)` - Euclidean distance
- Mood thresholds (line 1440):
  - Anxiety: browRatio < 0.9 && mar < 0.25
  - Fatigue: ear < 0.15 && mar > 0.3
  - Focus: ear > 0.35 && mar < 0.1
  - Neutral: mar > 0.35

**Mood enum (line 631):**
```javascript
const Mood = {
  FATIGUE: 'fatigue',
  ANXIETY: 'anxiety',
  FOCUS: 'focus',
  NEUTRAL: 'neutral',
  UNKNOWN: 'unknown'
};
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AudioEngine unit tests</name>
  <files>tests/audio-engine.test.js</files>
  <behavior>
    - Test createWhiteNoiseBuffer returns buffer with correct length
    - Test createWhiteNoiseBuffer values are in range [-1, 1]
    - Test createPinkNoiseBuffer returns buffer with correct length
    - Test createPinkNoiseBuffer has lower高频 content than white noise
    - Test setMood changes gain values for different moods
    - Test dispose cleans up audio nodes without errors
  </behavior>
  <action>
    Create tests/audio-engine.test.js with:

    **Mock Web Audio API:**
    ```javascript
    global.AudioContext = class {
      constructor() { this.sampleRate = 44100; }
      createBuffer(channels, size, sampleRate) {
        return { numberOfChannels: channels, length: size, sampleRate };
      }
      createBufferSource() { return { buffer: null, loop: true, start: () => {} }; }
      createBiquadFilter() { return { type: '', frequency: { value: 0 }, Q: { value: 0 } }; }
      createGain() { return { gain: { value: 0 } }; }
      get destination() { return {}; }
    };
    ```

    **Tests for buffer creation:**
    - Instantiate AudioEngine and call init() (after mocking)
    - Call createWhiteNoiseBuffer and verify output
    - Call createPinkNoiseBuffer and verify output
    - Call createRainBuffer and verify output

    **Tests for mood-based adjustments:**
    - Create AudioEngine instance with gains object pre-populated
    - Call setMood(Mood.ANXIETY) and verify gains change
    - Call setMood(Mood.FOCUS) and verify different gains
    - Call setMood(Mood.NEUTRAL) and verify gains change
  </action>
  <verify>
    <automated>npm test -- --run --reporter=verbose 2>&1 | grep -E "(✓|✗|PASS|FAIL)" | head -20</automated>
  </verify>
  <done>AudioEngine has passing tests for buffer creation and mood gain adjustment</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FaceAnalyzer unit tests</name>
  <files>tests/face-analyzer.test.js</files>
  <behavior>
    - Test calculateEAR with known eye landmarks returns correct ratio
    - Test calculateMAR with known mouth landmarks returns correct ratio
    - Test calcBrowEyeRatio with known landmarks returns correct ratio
    - Test dist calculates Euclidean distance correctly
    - Test mood classification: anxiety when browRatio < 0.9 && mar < 0.25
    - Test mood classification: fatigue when ear < 0.15 && mar > 0.3
    - Test mood classification: focus when ear > 0.35 && mar < 0.1
    - Test mood classification: neutral when mar > 0.35
  </behavior>
  <action>
    Create tests/face-analyzer.test.js with:

    **Test EAR calculation:**
    - Create mock eye landmarks array (6 points as {x, y} objects)
    - Call calculateEAR with mock eye
    - Verify return value is a number between 0 and 1
    - Test with closed eye (smaller EAR) vs open eye (larger EAR)

    **Test MAR calculation:**
    - Create mock mouth landmarks array (8 points as {x, y} objects)
    - Call calculateMAR with mock mouth
    - Verify return value is a number

    **Test browEyeRatio calculation:**
    - Create mock brow (5 points) and eye (6 points) arrays
    - Call calcBrowEyeRatio
    - Verify return value is positive number

    **Test mood classification thresholds:**
    - Test detect() method logic by manually testing the threshold conditions
    - Create FaceAnalyzer instance without video dependency
    - Override detect logic to test mood classification

    Note: detect() method requires face-api.js and video element which are hard to mock in unit tests. Focus on testing the calculation methods and threshold logic separately.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=verbose 2>&1 | grep -E "(✓|✗|PASS|FAIL|AudioEngine|FaceAnalyzer)" | head -30</automated>
  </verify>
  <done>FaceAnalyzer has passing tests for EAR/MAR/browEyeRatio calculations and mood thresholds</done>
</task>

</tasks>

<verification>
- Run `npm test -- --run` to execute all tests
- Verify all tests pass (no failures)
- Verify test coverage includes both AudioEngine and FaceAnalyzer
</verification>

<success_criteria>
1. AudioEngine tests pass: buffer creation, mood gain adjustment
2. FaceAnalyzer tests pass: EAR, MAR, browEyeRatio calculations
3. Mood classification threshold tests pass
4. Both test files have at least 50 lines of test code
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing/03-02-SUMMARY.md`
</output>
