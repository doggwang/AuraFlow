---
phase: 03-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.js
  - tests/setup.js
  - tests/audio-engine.test.js
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "npm test runs Vitest successfully"
    - "Vitest discovers and runs test files"
    - "Test environment is configured with proper globals"
  artifacts:
    - path: package.json
      provides: npm test script and Vitest dependency
      min_lines: 1
    - path: vitest.config.js
      provides: Vitest configuration with globals setup
      min_lines: 1
    - path: tests/setup.js
      provides: Extracts classes from index.html for testing
      min_lines: 30
---

<objective>
Establish Vitest testing framework for AuraFlow

Purpose: Enable automated testing for the vanilla JS codebase by setting up npm + Vitest with proper configuration to extract and test classes from the single-file HTML application.

Output: package.json, vitest.config.js, tests/setup.js, initial test file
</objective>

<context>
@D:/Project/vibe coding/AuraFlow/index.html (lines 631-637: Mood enum, lines 1068-1325: AudioEngine class, lines 1327-1507: FaceAnalyzer class)

**Key classes to test:**

Mood enum (line 631):
```javascript
const Mood = { FATIGUE: 'fatigue', ANXIETY: 'anxiety', FOCUS: 'focus', NEUTRAL: 'neutral', UNKNOWN: 'unknown' };
```

AudioEngine (line 1068): Creates noise buffers using Web Audio API, manages gain nodes and filters.

FaceAnalyzer (line 1327): Calculates EAR, MAR, browEyeRatio for mood detection using face-api.js.

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json with Vitest</name>
  <files>package.json</files>
  <action>
    Create package.json in project root with:
    - name: "auraflow"
    - version: "1.0.0"
    - private: true
    - scripts: { "test": "vitest" }
    - devDependencies: { "vitest": "^1.x" }

    This adds npm + Vitest to a project that previously had no package.json.
  </action>
  <verify>
    <automated>cat package.json | grep -q '"test": "vitest"' && echo "OK"</automated>
  </verify>
  <done>package.json exists with vitest dependency and npm test script</done>
</task>

<task type="auto">
  <name>Task 2: Create vitest.config.js</name>
  <files>vitest.config.js</files>
  <action>
    Create vitest.config.js with:
    - globals: true (for describe/it/expect globals)
    - environment: "jsdom" (for DOM testing)
    - setupFiles: ["./tests/setup.js"]
    - include: ["tests/**/*.test.js"]
    - reporters: ["default"]

    This configures Vitest to work with the vanilla JS project that uses DOM and global Mood/AudioEngine/FaceAnalyzer classes.
  </action>
  <verify>
    <automated>cat vitest.config.js | grep -q "setupFiles" && echo "OK"</automated>
  </verify>
  <done>vitest.config.js exists with proper configuration</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/setup.js</name>
  <files>tests/setup.js</files>
  <action>
    Create tests/setup.js that:
    1. Reads index.html as text
    2. Extracts the Mood enum and class definitions using regex/regex
    3. Evaluates them in a controlled scope
    4. Exposes Mood, AudioEngine, FaceAnalyzer as globals for test files

    This approach keeps the original single-file HTML intact while enabling testing.

    Key extraction patterns:
    - `const Mood = {[^}]+}` - extracts Mood enum
    - `class AudioEngine {[\s\S]*?^\s*}` - extracts AudioEngine class
    - `class FaceAnalyzer {[\s\S]*?^\s*}` - extracts FaceAnalyzer class
  </action>
  <verify>
    <automated>cat tests/setup.js | grep -q "Mood" && cat tests/setup.js | grep -q "AudioEngine" && cat tests/setup.js | grep -q "FaceAnalyzer" && echo "OK"</automated>
  </verify>
  <done>tests/setup.js extracts all classes and exposes them as globals</done>
</task>

</tasks>

<verification>
- Run `npm install` to install Vitest
- Run `npm test -- --run` to verify tests execute (will show 0 tests until plan 03-02)
- Verify no errors in console output
</verification>

<success_criteria>
1. npm test runs Vitest and exits successfully
2. Vitest discovers test files in tests/ directory
3. Test environment has Mood, AudioEngine, FaceAnalyzer available globally
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing/03-01-SUMMARY.md`
</output>
